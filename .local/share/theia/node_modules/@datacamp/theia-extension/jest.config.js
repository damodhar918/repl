module.exports = {
  collectCoverageFrom: ['src/**/*.ts', '!**/*.d.ts'],
  globals: {
    'ts-jest': {
      tsConfig: './tsconfig.json',
    },
  },
  moduleFileExtensions: ['ts', 'js'],
  moduleNameMapper: {
    '\\.(css|less|scss|sass)$': 'identity-obj-proxy',
    // Fix to run tests that import vscode
    'editor-command': 'identity-obj-proxy',
    'editor-frontend-module': 'identity-obj-proxy',
  },
  preset: 'ts-jest',
  setupFiles: ['<rootDir>/src/setupTests.ts'],
  testEnvironment: 'jsdom',
  testMatch: ['**/*.spec.ts'],
};
